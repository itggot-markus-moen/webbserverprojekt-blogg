a href="/" Back

-if info != nil
    h1 = "#{info["Title"]}"

    -info["Data"].each do |e|
        h3 = "#{e["Title"]}"
        p = "#{e["Text"]}"
        / a href="/list/#{e["id"]}" See User

    a href="/makepost" Make a post

-else
    p = "Whoops, there's nothing here #{session[:account]["Username"]}..."
    a href="/blogmake" Get started